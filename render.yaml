services:
  - type: web
    name: pos-backend
    # ... (rest of web service config)
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: my-old-database # <-- Change this name
          property: connectionString
      # ... (rest of env vars)

databases:
  - name: my-old-database # <-- And change this name
    plan: free
